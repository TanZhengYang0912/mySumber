targets:
  $default:
    builders:
      drift_dev:
        options:
          databases:
            local_database: lib/core/local_database/local_database.dart
          schema_dir: test/drift_schemas
          test_dir: test/drift
